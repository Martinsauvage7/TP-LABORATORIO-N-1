#include <stdio.h>
#include <stdlib.h>

float Division(float * resultado, int primerNumero,int segundoNumero);

int main(void)
{
	setbuf(stdout, NULL);
	float resultado;
	int primerNumero;
	int segundoNumero;

	printf("Ingrese numero 1\n");
	fflush(stdin);
	scanf("%d",&primerNumero);
	printf("Ingrese numero 2\n");
	fflush(stdin);
	scanf("%d",&segundoNumero);

		resultado=Division (&resultado,primerNumero/segundoNumero);
		if(respuesta ==0)
			{
				return retorno;
			}
		else
			{
			printf("Error.");
			}
		return 0;
}

float Division(float* resultado, int n1,int n2)
	{
	float resultado;
	int retorno=-1;

if(resultado =! NULL || (float)n2=!0)
{
	resultado=(float)n1/n2;
*resultado= resultado;
return 0;
}
else
{
printf("Error");
return retorno;
}







